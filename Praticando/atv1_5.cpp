//Ordem de precedência entre variáveis oprando soma e multiplicação
#include <stdio.h>
#include <stdlib.h>
int main(){
    	int A, B, C;
    		scanf("%d", &A);
    		scanf("%d", &B);
    		C = A + B * B;
    		printf("O resultado é %d", C);
}
